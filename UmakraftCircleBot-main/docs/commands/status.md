# /status

Show live bot health, sync status, and uptime.

## Options
None.

## Behavior
- Posts an embed (visible to everyone) with:
  - Memory usage (heap / RSS)
  - Bot uptime
  - Last sync time per circle
  - Task health summary (last run, consecutive failures)

## Example
```
/status
```
