# /fan_gain

Purpose
- Show recent fan gains for a trainer or circle.

Options
- trainer_id or circle_id
- period (days)

Behavior
- Uses Vault snapshots or uma.moe rankings endpoints to compute gains
- Returns a small card or embed with sparkline and top gainers
